import Projects from "./projects";

const Portfolio = () => {
    return(
        <section id="portfolio">
            <h2 id="section-title">Portfólio</h2>
            <span id="section-subtitle">Meus projetos</span>

            <Projects />
        </section>
    )
}

export default Portfolio;